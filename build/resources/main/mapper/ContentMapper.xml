<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ssd.springcooler.gachiwatch.dao.mybatis.mapper.ContentMapper">

    <!-- 1. createContent -->
    <insert id="createContent" parameterType="ssd.springcooler.gachiwatch.domain.Content">
        INSERT INTO content (content_id, title, intro, casts, rate, img_url, content_type, upload_date)
        VALUES (#{contentId}, #{title}, #{intro}, #{cast}, #{rate}, #{imgUrl}, #{contentType}, #{uploadDate})
    </insert>

    <insert id="insertGenre" parameterType="int">
        INSERT INTO content_genre (content_id, genre_id)
        VALUES (#{contentId}, #{genreId})
    </insert>

    <insert id="insertPlatform" parameterType="int">
        INSERT INTO content_ott (content_id, platform_id)
        VALUES (#{contentId}, #{platformId})
    </insert>

    <!-- 2. getContentList -->
    <select id="getContentList" parameterType="list" resultType="ssd.springcooler.gachiwatch.domain.Content">
        SELECT
        content_id AS contentId,
        title,
        intro,
        casts AS cast,
        rate,
        upload_date AS uploadDate,
        content_type AS contentType,
        img_url AS imgUrl
        FROM content
        WHERE content_id IN
        <foreach collection="list" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <select id="getGenresByContentId" resultType="int" parameterType="int">
        SELECT genre_id FROM content_genre WHERE content_id = #{contentId}
    </select>

    <select id="getPlatformsByContentId" resultType="int" parameterType="int">
        SELECT platform_id FROM content_ott WHERE content_id = #{contentId}
    </select>

    <!-- 4. findById -->
    <select id="findById" parameterType="int" resultType="ssd.springcooler.gachiwatch.domain.Content">
        SELECT
        content_id AS contentId,
        title,
        intro,
        casts AS cast,
        rate,
        upload_date AS uploadDate,
        content_type AS contentType,
        img_url AS imgUrl
        FROM content
        WHERE content_id = #{id}
    </select>



</mapper>